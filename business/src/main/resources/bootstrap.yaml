spring:
  application:
    name: business
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        namespace: train
        server-addr: localhost:8848
      config:
        namespace: train
        server-addr: localhost:8848
        file-extension: yaml

