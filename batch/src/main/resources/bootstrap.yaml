spring:
  application:
    name: batch
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        namespace: train
        server-addr: 127.0.0.1:8848
      config:
        namespace: train
        server-addr: 127.0.0.1:8848
        file-extension: yaml

